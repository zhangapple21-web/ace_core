# ACE “捡了芝麻丢了西瓜”收口证据

时间：2026-09-08 Asia/Shanghai

## 当前状态（运行审计实测）

- `data_health=BLOCKED`：`stock_data_benchmark_latest.json` 有 1 个观测轮次，核心来源覆盖率仍为 0，不能宣称 READY。
- `advisor=NOT_READY`：唯一 `runner_status.json` 位于 mine-seed 外部仓，最后运行 2026-07-14 且 `last_run_success=false`，审计判定为外部陈旧证据。
- `risk=NOT_READY`：生产 `risk_status.json` 缺失或格式不可读。
- `video`：ACE daemon 仅能记录 `CARD_QUEUED`/派发结果；可播放性必须以实际文件、字节数、SHA-256、媒体探针和可访问 URL 复核，不能以卡片派发代替交付。

## 已执行动作

1. 运行 `python ops/run_autonomous_audit.py`，生成当日 `daily_health.json`、`daily_health.md`、`blocking_reasons.json`、`trend_report.json`。
2. 固化本证据，明确三项阻断的事实边界，禁止把历史 advisor 运行、研究卡或视频卡片写成当前可运行状态。
3. 自由区与考古继续保持隔离：只接受真实模型活动/可验证实验及能落到规则、修复、复用证据的产物。

## 可复用收口规则

- 股票：只有新鲜、可审计的多源覆盖与准入证据才能解除 BLOCKED；数据不足输出 `RESEARCH_ONLY`。
- 视频：状态机必须至少区分 `CARD_QUEUED`、`GENERATING`、`MEDIA_VERIFIED`、`DELIVERY_READY`、`DELIVERY_FAILED`；`DELIVERY_READY` 需要重新计算媒体和访问门。
- 自由区/考古：没有真实活动或可执行落点的材料不进入生产路由。
